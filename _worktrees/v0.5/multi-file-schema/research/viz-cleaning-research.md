# ThaiEDA Research: Data Visualization & Data Cleaning

> Auto-generated by cron research job. ห้ามนำไปใช้เป็น source code — เป็น research notes เท่านั้น

## 2026-06-25

### Ridgeline plots & violin plots — ทางเลือกแทน histogram สำหรับ distribution หลายกลุ่ม

Ridgeline plot (joy plot) แสดง density distribution ของหลายกลุ่มซ้อนกันในแนวนอน เห็นการเปลี่ยนแปลง shape ระหว่างกลุ่มได้ชัดกว่า histogram แบบ separate ส่วน violin plot รวม box plot + kernel density ไว้ในรูปเดียว เหมาะกับ distribution ที่ไม่ใช่ normal (bimodal, skewed) — ปัญหาที่พบบ่อยในข้อมูลไทย เช่น อายุ/รายได้/ความยาวข้อความ ThaiEDA ใช้ histogram + box plot อยู่ ต่อยอดให้ auto-select violin เมื่อตรวจพบ bimodal distribution จะเพิ่ม insight ได้ดี

Source: https://www.datylon.com/blog/types-of-charts-graphs-examples-data-visualization

Source: https://medium.com/@tarammullin/python-data-visualization-for-exploratory-data-analysis-eda-fafcf6ecdadc

### stylecloud — word cloud สวยขึ้น + ทางเลือกแทน wordcloud library

stylecloud ต่อยอดจาก wordcloud โดยใช้ icon shape (Font Awesome) เป็น mask เปลี่ยน palette ได้ง่าย (palettable) และเก็บ stopword removal อัตโนมัติ ปัญหา Thai word cloud ใน library ดั้งเดิมคือ vowel บน/ล่างขาดเพราะ font ไม่รองรับ combining character (issue #448) ทางเลือกคือสร้าง co-occurrence network graph หรือ treemap แทน เพื่อหลีกหนีข้อจำกัด rendering ของ wordcloud ต่อภาษาตัวซ้อน — ThaiEDA ที่ใช้ Sarabun font อยู่ควรพิจารณา stylecloud + ตรวจ font ที่รองรับ combining mark แบบเต็ม

Source: https://pypi.org/project/stylecloud

Source: https://github.com/amueller/word_cloud/issues/448

Source: https://towardsdatascience.com/beyond-the-cloud-4-visualizations-to-use-instead-of-word-cloud-960dd516f215

### ftfy — แก้ mojibake และ encoding mix-up อัตโนมัติ

ftfy ตรวจจับ pattern ของ UTF-8 ที่ถูก decode ผิด (เช่น ภาษาไทยที่กลายเป็น `à¸ªà¸§à¸±à¸ª`) แล้วแก้กลับอัตโนมัติ โดยใช้ heuristics ที่รู้จัก encoding ทั่วไป สำหรับข้อมูลไทยที่มาจาก CSV/Excel หลายแหล่ง มักเจอ mixed encoding ใน column เดียว — ftfy จัดการต่อบรรทัดได้ แต่มีข้อจำกัด: ไม่รองรับ mixed encoding ในบรรทัดเดียว (GitHub issue #51) ต้อง split ก่อน ใช้คู่กับ fuzzy matching (fuzzywuzzy/rapidfuzz) สำหรับ dedup ชื่อไทยที่มีช่องว่าง/วรรณยุกต์ไม่ตรงกัน

Source: https://pypi.org/project/ftfy

Source: https://github.com/LuminosoInsight/python-ftfy/issues/51

Source: https://www.getflookup.com/blog/python-data-cleaning-fuzzy-matching

### ydata-profiling vs Sweetviz — data quality profiling ที่ ThaiEDA ต้องเรียนรู้

ydata-profiling (เดิม pandas-profiling) สร้าง HTML report แบบ inline แสดง per-column stats, correlations, missing matrix, warnings พร้อม actionable flag (high cardinality, skewed, constant) Sweetviz เน้น dataset comparison + target association ด้วย visualization ที่สวยและ compact จุดที่ ThaiEDA ยังขาด: auto-flag severity (critical/warning/info), correlation heatmap ที่กรองเฉพาะ strong correlation, และ dataset diff view สำหรับเปรียบเทียบข้อมูลก่อน/หลัง cleaning — น่าเอามาเป็น UX reference สำหรับ report layout รุ่นถัดไป

Source: https://ydata-profiling.ydata.ai

Source: https://medium.com/@harishk3493/effortless-eda-with-sweetviz-ydata-profiling-secret-weapons-for-every-data-scientist-1c03625e4e8c

### missingno — visualize รูปแบบ missing data ระดับ matrix และ nullity correlation

missingno ให้ 4 view หลัก: matrix plot (เห็นตำแหน่ง missing เป็นแถบขาว), bar chart (จำนวน non-null ต่อ column), heatmap (nullity correlation ระหว่าง column -1 ถึง 1) และ dendrogram (cluster column ที่ missing pattern เหมือนกัน) nullity correlation สำคัญเพราะบอกว่า missing เป็นแบบ MNAR/MAR/MCAR — เช่น ถ้า column A หายเมื่อ column B มีค่า แปลว่าไม่ใช่สุ่ม ThaiEDA ตอนนี้มี quality checks แต่ไม่ได้ visualize pattern ของ missing การเพิ่ม matrix plot + nullity heatmap จะทำให้ user เห็นปัญหาที่ตัวเลขอย่างเดียวมองข้าม และช่วยตัดสินใจว่าควร impute หรือ drop

Source: https://www.nb-data.com/p/how-to-visualize-missing-data-patterns

Source: https://book.the-turing-way.org/project-design/missing-data/missing-data-visualising-missingness