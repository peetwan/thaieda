You are fixing defects in ThaiEDA, an open-source AutoEDA library for Thai data.

## Project
- Path: C:\Users\User\Desktop\Hermes_Peet\scratch\thaieda
- Source: src/thaieda/
- Tests: tests/ (424 tests, 24 skipped)
- venv: .venv-thaieda/Scripts/python.exe
- Python 3.10+, type hints, Thai docstrings/comments
- Run tests: PYTHONUTF8=1 PYTHONPATH="src" ".venv-thaieda/Scripts/python.exe" -m pytest tests/ -x -q -o "addopts="
- Lint: ".venv-thaieda/Scripts/python.exe" -m ruff check src/ tests/ && ".venv-thaieda/Scripts/python.exe" -m ruff format src/ tests/

## Your task: Fix ALL defects in Batch 1 (Clean/Quality module)

### Defect C1: Placeholder over-flagging on "-" (single dash)
- File: src/thaieda/quality/__init__.py — function `check_placeholder_values` (line ~676)
- Problem: `"-"` ใน `_PLACEHOLDER_SET` ทำให้ทุกคอลัมน์ที่มี "-" ถูก flag เป็น placeholder
  Superstore มี 302 ค่า "-" ใน 13 คอลัมน์ → 13 insights ที่ซ้ำกัน
- Fix: ถ้า placeholder ที่พบเป็นแค่ "-" (single dash) และคิดเป็น <1% ของ column → ข้าม
  ถ้าพบหลาย placeholder types หรือ >5% → flag ปกติ
  เป้าหมาย: ลด false positive บนข้อมูล English ที่ใช้ "-" แทน missing บางครั้ง

### Defect C2: repeated-char spam false positive on ID columns
- File: src/thaieda/quality/__init__.py — function `_skip_repeated_spam_check` (line ~400)
- Problem: Product ID 8,038 แถว (78.1%) ถูก flag ว่ามี "repeated-char spam" normalization issue
  ทั้งที่ Product ID เป็น code ปกติ เช่น "FUR-BO-10001798"
- Root cause: `_skip_repeated_spam_check` skip เฉพาะ text <15 chars
  แต่ Product ID ยาวกว่านั้น และมี pattern ที่ trigger `_REPEAT_SPAM_RE`
- Fix: ขยาย skip rule — skip ถ้าเป็น ID-like pattern:
  - contains digits + hyphens (code format: XXX-XX-99999999)
  - alphanumeric code with hyphens (not natural text)
  - มีตัวเลข >=3 ตัวและ hyphen >=1

### Defect D1: Order ID ถูกระบุเป็น date column
- File: src/thaieda/detect/__init__.py — function `_looks_like_datetime` (line ~713)
- Problem: Superstore "Order ID" (CA-2017-152156) ถูกระบุว่ามี "5 รูปแบบวันที่"
- Root cause: pattern CA-9999-9999 ถูก parse เป็น date ได้เพราะมี 9999-9999 segment
- Fix: เพิ่ม guard — ถ้าค่าในคอลัมน์มี alphabetic prefix 2+ ตัวก่อนตัวเลข (CA-, US-, OFF-) → ไม่ใช่ date
  ตรวจว่า prefix เป็นตัวอักษรล้วนก่อน segment แรกที่มีตัวเลข

### Defect R1: Cross-Column Insights labels ว่าง (Breakdown/Measure)
- File: src/thaieda/report/_template.py (line ~355)
- Problem: template แสดง "Breakdown: · Measure:" แม้ perspective.breakdown จะว่าง
- Current template (line 355):
  `<span class="cat ng">{{ L('breakdown') }}: <code>{{ c.perspective.breakdown }}</code>{% if c.perspective.measure %} · {{ L('measure') }}: <code>{{ c.perspective.measure }}</code>{% endif %} · {{ c.perspective.agg }}</span>`
- Fix: เพิ่ม {% if c.perspective.breakdown %} guard รอบ Breakdown label เหมือน measure:
  `{% if c.perspective.breakdown %}{{ L('breakdown') }}: <code>{{ c.perspective.breakdown }}</code>{% endif %}{% if c.perspective.measure %} · {{ L('measure') }}: <code>{{ c.perspective.measure }}</code>{% endif %} · {{ c.perspective.agg }}`

### Defect U1: Date parsing warnings
- Files: หาจุดที่เรียก pd.to_datetime() โดยไม่ส่ง format= ใน src/thaieda/
- Problem: `UserWarning: Could not infer format` ทุก dataset ที่มี date column
- Fix: ใช้ `pd.to_datetime(series, format='mixed')` หรือ `errors='coerce'` + กัน warning ด้วย warnings.catch_warnings

## Constraints
1. ห้าม break existing tests — run pytest ก่อนและหลังแก้
2. ใช้ vectorized operations (pandas .str accessors) ไม่ใช่ row-by-row loops
3. Thai docstrings/comments
4. No silent fallbacks
5. Type hints required
6. อ่าน existing code ก่อนแก้ เพื่อ match patterns

## Steps
1. Read src/thaieda/quality/__init__.py — understand check_placeholder_values + _skip_repeated_spam_check
2. Read src/thaieda/detect/__init__.py — understand _looks_like_datetime
3. Read src/thaieda/report/_template.py — understand line 355
4. Run pytest tests/test_quality.py tests/test_detect.py tests/test_report.py -x -q -o "addopts=" to get baseline
5. Fix C1 (placeholder threshold)
6. Fix C2 (ID pattern skip)
7. Fix D1 (alphabetic prefix guard)
8. Fix R1 (template guard)
9. Fix U1 (date format warning)
10. Run full test suite: ".venv-thaieda/Scripts/python.exe" -m pytest tests/ -x -q -o "addopts="
11. Run ruff check + format
12. Report: files changed, test results (before/after), any tests that broke

## Report format
- Files modified (list)
- Tests before: X passed, Y failed
- Tests after: X passed, Y failed
- Ruff: clean or errors
- Each fix: what changed + why